interface:
  display_name: "Ops Dashboard Skill"
  short_description: "Run the bundled local dashboard script and return the generated HTML path/URL. Do not hand-write HTML."
  default_prompt: "Use $ops-dashboard-skill by executing `python3 scripts/run_ops_dashboard.py`. You must use the bundled HTML template, must not generate a custom HTML page yourself, and must return the exact htmlPath/url from script output."
